---
import type { ComponentProps } from "astro/types";
import { Image } from "astro:assets";

type Props = ComponentProps<typeof Image>;

const props = Astro.props;
---

<Image {...props} data-state="article" />
<figcaption>
    {props.alt}
</figcaption>
